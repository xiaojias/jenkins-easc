folder('Inital-JCasC')
pipelineJob('Inital-JCasC/job-dsl-plugin') {
  definition {
    cpsScm {
      scm {
        git {
          remote {
            url('git@github.com:xiaojias/devops-cicd.git')
            credential('credential-github-xiaojias')
          }
          branch('*/wip')
          script('app01.Jenkinsfile')
        }
      }
      lightweight()
    }
  }
}

